#Solve a quadratic expression

a <- -8
b <- 14
c <- 15

solution_1 <- (-b + (b^2-4*a*c)^(1/2))/(2*a)
solution_2 <- (-b - (b^2-4*a*c)^(1/2))/(2*a)

#Sum of first integers

max_int <- 1000

sum_of_int <- (max_int*(max_int+1))/2

sum_of_int

#Trying a rare code

sequence_sum <- seq(1, max_int)
sum(sequence_sum)
sequence_sum

#Evaluate sqrt and log

log(sqrt(100), base= 10)